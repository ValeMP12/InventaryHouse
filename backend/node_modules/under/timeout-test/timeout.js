var http = require('http');

var delay = 3000,
    port  = 3000;

http.createServer(function(req, res) {
  console.log(req.method + ' ' + req.url)
  setTimeout(function() {
    res.end('OK');
  }, delay);
}).listen(port);